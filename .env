APP_HOST=0.0.0.0
APP_PORT=8000
ENV=dev

# Modes: keep stub to avoid heavy installs for first run
ASR_MODE=stub
LLM_MODE=openai
TTS_MODE=stub

# If you don't have a key, it's fine; we’ll fall back to rule-based replies
LLM_API_KEY=

# SQLite DB (auto-created)
DB_URL=sqlite:///./data/poc.db

# Leave Redis blank to use in-memory sessions (easiest)
REDIS_URL=

WEBHOOK_SECRET=changeme
MEDIA_BASE_URL=http://localhost:8000/media
LOG_LEVEL=info
